# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Datasets and training artifacts (regeneratable)
tsr_train/data/
tsr_train/runs/
tsr_train/Reference/

# Detector dataset / training artifacts (huge; regeneratable via detector scripts).
# detector.onnx already ships in TsrApp/Assets, so exports/ is not committed.
tsr_train/detector/data/
tsr_train/detector/runs/
tsr_train/detector/exports/

# Model weights (detector.onnx ships in Assets; .pt are regeneratable)
*.pt

# .NET / C#
bin/
obj/
*.user
*.suo
*.userprefs
.vs/
*.cache

# Published app build (regeneratable via `dotnet publish`)
TsrApp/publish/
TsrApp/publish.rar

# Sample / input videos (large binaries, not source)
видео для Tsr_app/

# Runtime prediction log (per-machine, not source)
predictions_log.csv

# IDE
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store
desktop.ini
